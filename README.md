Telegram Bot - README
Description
This project is a simple Telegram bot written in Python using the python-telegram-bot library. The bot supports two main features:

Responds to the /start command by greeting the user and logging basic user information.
Echoes any text message sent by the user, replying with the same content.
